import Vue from 'vue';
import * as ShortKey from 'vue-shortkey';

// add any custom shortkey config settings here
// for example Vue.use(ShortKey, { prevent: ['input', 'textarea'] })
Vue.use(ShortKey);
